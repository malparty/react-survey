name: Test

on: push

jobs:
  build:
    runs-on: ubuntu-latest
      with:
        node-version: [16.4]

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Install modules
      run: yarn

    - name: Run tests
      run: yarn test
